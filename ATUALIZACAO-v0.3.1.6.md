# Resenha FC v0.3.1.6

Correção exclusiva do login Google.

- removido o iframe visual do Google Identity Services;
- removidos máscara, recorte, blend mode e deslocamento do botão;
- botão totalmente nativo da interface, com fundo integrado ao tema;
- símbolo G vetorial transparente;
- autenticação acionada pelo fluxo Google OAuth do Supabase;
- demais funções e banco de dados sem alteração;
- cache atualizado para `resenha-fc-v0.3.1.6`.

Substitua `app.js`, `styles.css`, `index.html` e `service-worker.js`. Preserve `supabase-config.js`.
